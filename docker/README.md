# docker

Reserved for future Postgres init scripts, pgvector setup (Phase 6), and any auxiliary
container configuration that doesn't belong in the root `docker-compose.yml` or an app's
own `Dockerfile`. Empty in Phase 1.

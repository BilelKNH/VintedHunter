# tests

Reserved for the cross-package / end-to-end test suite (Playwright, per spec §93-94),
expected to land around Phase 3 (dashboard) or Phase 7 (optimisation/CI). Per-package unit
tests live alongside their source (e.g. `apps/api/src/*.test.ts`) instead. Empty in Phase 1.
